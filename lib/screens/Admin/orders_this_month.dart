import 'package:flutter/material.dart';

class OrdersOfMonth extends StatelessWidget {
  const OrdersOfMonth({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
